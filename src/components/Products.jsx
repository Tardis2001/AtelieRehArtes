import { useRef } from "react";

export default function Products() {
    const section1Ref = useRef(null);
    return <div className="w-full h-screen flex justify-center items-center bg-slate-400">
        <h1>Products</h1>

    </div>

}